# PerformanceDashboard

## GraphCache生成

依存パッケージをインストール後、リポジトリ直下で実行します。

```text
python generate_graphs.py
```

生成済みのグラフは `GraphCache/` に保存されます。

## Dashboard起動

GraphCacheを生成した後、リポジトリ直下でStreamlitを起動します。

```text
python generate_graphs.py
streamlit run app.py
```
